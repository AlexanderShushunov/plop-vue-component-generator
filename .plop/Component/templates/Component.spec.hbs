{{#if vuex}}import Vuex from 'vuex'
{{/if}}import { shallowMount{{#if vuex}}, createLocalVue{{/if}} } from '@vue/test-utils'
import {{> ComponentName}} from './{{> ComponentName}}'
{{#if vuex}}
import { createStubStore } from './createStubStore'

const localVue = createLocalVue()
localVue.use(Vuex)

const store = createStubStore()
{{/if}}

describe('{{> ComponentName}}', () => {
  it('Render', () => {
    const wrapper = shallowMount({{> ComponentName}}, {
      {{#if vuex}}
      localVue,
      store,
      {{/if}}
      propsData: { }
    })
    expect(wrapper.html()).toMatchSnapshot()
  })
})
