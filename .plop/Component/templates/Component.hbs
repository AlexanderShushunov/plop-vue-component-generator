<template>
  <div class="{{> CssClassName}}">
    {{#if vuex}}
      \{{field}}
    {{/if}}
  </div>
</template>

<script>
{{#if vuex}}
import { mapState } from 'vuex'
{{/if}}

export default {
  name: '{{> ComponentName}}',
  {{#if vuex}}
  computed: {
    ...mapState('module', [
      'field'
    ])
  }
  {{/if}}
}
</script>

<style scoped>
  .{{> CssClassName}} {
  }
</style>
